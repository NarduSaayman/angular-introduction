import { FilterGradsPipe } from './filter-grads.pipe';

describe('FilterGradsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGradsPipe();
    expect(pipe).toBeTruthy();
  });
});
